# prometheus.rules.yml
groups:
- name: blackbox_http
  rules:
  - alert: StaticSiteDown
    expr: probe_success{job="blackbox-exporter"} == 0
    for: 30s
    labels:
      severity: critical
    annotations:
      summary: "網站 {{ $labels.instance }} 無法存取"
      description: "Blackbox probe 連線失敗"
